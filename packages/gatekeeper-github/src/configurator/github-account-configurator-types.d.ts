export type GitHubAccountConfiguratorValues = Record<string, never>;

/** The account resource needs no configuration inputs, so no RPC surface either. */
export interface GitHubAccountConfiguratorRpc {}
